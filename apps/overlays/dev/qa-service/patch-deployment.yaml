# apps/overlays/dev/qa-service/patch-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: qa-service
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: qa-service
        # Example: Set resource limits for the environment
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
        # Inject environment-specific configuration
        envFrom:
        - configMapRef:
            name: qa-service-config-dev
        # - secretRef:
        #     name: qa-service-secrets-dev
